import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // The app is fully client-side (localStorage only). React Strict Mode stays on.
  reactStrictMode: true,
}

export default nextConfig
